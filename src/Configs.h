// WiFi Configs
#define WIFI_SSID "<Your WiFi Name>"
#define WIFI_PASSWORD "<Your WiFi Password>"

// AWS IoT Configs
#define AWS_IOT_ENDPOINT  "<Your IoT Endpoint>"
#define AWS_CLIENT_ID "<Your Device Client ID>"

static const char * AWS_ROOT_CA1 = R"EOF(

)EOF";

static const char * AWS_DEVICE_CERT = R"EOF(

)EOF";

static const char * AWS_DEVICE_PRIVATE_CERT = R"EOF(

)EOF";
